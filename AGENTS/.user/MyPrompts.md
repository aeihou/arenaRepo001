# MyPrompts.md

Prompt journal for `arenaRepo001` — every prompt sent in this session,
appended at EBT via `BeforeThinking.MyPrompts.md.Add(new Prompt)`.

- Kept in: `AGENTS/.user/MyPrompts.md`

---

### Prompt 1

```text
ConvertMyPseudocodeTo scripting "SelfConstructor.sh" selfConstructor(nameOfFolder().newFile("++**[nameOfFolder.md](http://nameOfFolder.md)**++")); newSession.Reload(); -convert mypseudocodeTo ++**[script.sh](http://script.sh)**++ parametrizable y ejecutar por primera vez-  selfConstructor selfdescribedRepo  initWorkspace();initSession(nameOfFolder.newFile.README); convert mypseudocodeto selfExecute script for sh   "initWorkspace();initSession(nameOfFolder.newFile.README);"  #!/bin/sh

# [constructor.sh](http://constructor.sh) - self-executing workspace initializer.  "selfConstructor(nameOfFolder().newFile("[nameOfFolder.md](http://nameOfFolder.md)")) "
```

- Added: (collected from session history)

### Prompt 2

```text
AGENTS/agents.md().Update($); arena_AI. commitAndPush
```

- Added: (collected from session history)

### Prompt 3

```text
mkdir(/SRC/Tools).selfConstructor($folderName().NewFile("$folderName.md");
```

- Added: (collected from session history)

### Prompt 4

```text
mkdir('/AGENTS/agents.md().selfConstructor("$ddmmaaaaHHMMSS")
```

- Added: (collected from session history)

### Prompt 5

```text
if (Not)nameOfFolder then mkdir("nameOfFolder: ddmmaaaaHHMMSS") .selfConstructor("$Name_Folder")
```

- Added: (collected from session history)

### Prompt 6

```text
mkdir().SelfConstructor("$Name_Folder = 'TEST'"().NewFile("$Name_Folder.md".SelfDescribe("README.md")); SelfDescriberRepo().Update({$thisRepo}); -Write to /AGENTS/agents.md addText("Section: ExecuteBeforeThinking.DescribeMe(auto)"
```

- Added: (collected from session history)

### Prompt 7

```text
cd /TEST/;mkdir(ddmmaahhmmss); copy ./[SelfConstructor.sh](http://SelfConstructor.sh) assign execution permissions and execute it
```

- Added: (collected from session history)

### Prompt 8

```text
**ddmmaaaaHHMMSS;** [SelfConstructor.sh](http://SelfConstructor.sh) --NameOfFolder:MyTest; Arena_AI.pushAndCommit();
```

- Added: (collected from session history)

### Prompt 9

```text
Actualizar(Para todo [nameofFolder.md](http://nameofFolder.md) OF README.md); push;
```

- Added: (collected from session history)

### Prompt 10

```text
Keep and POSIX: For EveryNewFileConstructor(); keep the command or script that created it
```

- Added: (collected from session history)

### Prompt 11

```text
Repo.Verify(consistency().Includes("duped OR outdated OR redundant OR missplaced")
```

- Added: (collected from session history)

### Prompt 12

```text
Keep **ExecuteAfterThinking.Commit&&Push(); ExecuteBeforeThinking.Synch(Repo); ExecuteAfterThinking = EAT and ExecuteBeforeThinking = EBT; ExportSession("userPrompt,log.init()");**
```

- Added: (collected from session history)

### Prompt 13

```text
self.Consistency.new(verify documentation to scripts)
```

- Added: (collected from session history)

### Prompt 14

```text
newFile(/AGENTS/PortableSessionAI.md); and write to: the skill procedures and data you export to another agent_AI to be able to continue this conversation/Development/Context
```

- Added: (collected from session history)

### Prompt 15

```text
Repo.Optimize(performance, hand-off timing);
```

- Added: (collected from session history)

### Prompt 16

```text
Repo.SearchForHardodedVariables().ReplaceFixedHarcodedVariables();
```

- Added: (collected from session history)

### Prompt 17

```text
Repo.Update(ForEveryFile);
```

- Added: (collected from session history)

### Prompt 18

```text
Please open a pull request for the changes on this branch.
```

- Added: (collected from session history)

### Prompt 19

```text
NewFile(/AGENTS/SessionHand-off.md).Write"The essential data and procedures for another agent to continue this session/context".()
```

- Added: (collected from session history)
### Prompt 20

```text
newFolder(/AGENTS/.user/); Keep there my ++**[MyPrompts.md](http://MyPrompts.md)**++().Keep all mypromts sent in this session; and generate a function BeforeThinking.MyPrompts.md().Add(new Prompt);
```

- Added: 2026-08-20T01:14:13Z (UTC)

